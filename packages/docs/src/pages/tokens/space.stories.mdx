import { Meta } from '@storybook/addon-docs';
import { TokensGrid } from '../../components/TokensGrid'
import { space } from '@palamar-ui/tokens'

<Meta title="Utilities/Tokens/Spaces" />

# Spaces

<p style={{
  fontSize: '0.95rem',
  fontWeight: 'normal',
  marginBottom: '1rem'
}}>
  Spaces are used to define the margin and padding of an element.
</p>

<TokensGrid tokens={space} hasRemValue />