import { Meta } from '@storybook/addon-docs';
import { TokensGrid } from '../../components/TokensGrid'
import { radii } from '@palamar-ui/tokens'

<Meta title="Utilities/Tokens/Radii" />

# Radii

<p style={{
  fontSize: '0.95rem',
  fontWeight: 'normal',
  marginBottom: '1rem'
}}>
  The radii tokens are used to define the border radius of elements.
</p>

<TokensGrid tokens={radii} />