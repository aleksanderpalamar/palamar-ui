import { Meta } from '@storybook/addon-docs';
import { TokensGrid } from '../../components/TokensGrid'
import { fontWeights } from '@palamar-ui/tokens'

<Meta title="Utilities/Tokens/Font Weights" />

# Font Weights

<p style={{
  fontSize: '0.95rem',
  fontWeight: 'normal',
  marginBottom: '1rem'
}}>
  Font weights are used to define the thickness of a font. They are used to create hierarchy and visual distinction between different elements.
</p>

<TokensGrid tokens={fontWeights} />