import { Meta } from '@storybook/addon-docs';
import { TokensGrid } from '../../components/TokensGrid'
import { fontSizes } from '@palamar-ui/tokens'

<Meta title="Utilities/Tokens/Font Sizes" />

# Font Sizes

<p style={{
  fontSize: '0.95rem',
  fontWeight: 'normal',
  marginBottom: '1rem'
}}>
  Font sizes are used to define the size of the text. They are used in combination with the font family and font weight.
</p>

<TokensGrid tokens={fontSizes} hasRemValue />